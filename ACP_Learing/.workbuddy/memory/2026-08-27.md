# 2026-08-27 工作日志

## ACP 大模型教程学习（C2_构造问答系统）

- 通读 2.1 笔记（`C2_构造问答系统/2_1_用大模型构建新人答疑机器人.ipynb`），梳理 C2 全章脉络（2.0~2.5）。
- 封装 `course_core/qwen_client.py`：把 2.1 的 OpenAI client 初始化 + 对话调用封装为 `chat()` / `chat_stream()`，采用懒加载 `_get_client()` + 自动 load_key 兜底，默认 system_prompt=公司小蜜、model=qwen-max、temperature=0.7、top_p=0.8。
- 解决 2.1 开头 `os.chdir(os.path.join(os.path.dirname(os.path.abspath('')), 'course_core'))` 的报错：
  - 根因：`os.path.abspath('')` 取的是内核当前工作目录(cwd)，若 cwd 已在 course_core 子目录内，会拼出 `course_core/course_core` 不存在的路径 → FileNotFoundError。
  - 解法（推荐）：用 Path.cwd() 向上逐级查找含 `config/load_key.py` 的目录再 os.chdir + sys.path.insert。
  - 用户实际运行环境是百炼 Linux（/mnt/workspace/...），非本地 Windows。

## 待办/后续
- 可选：把 1.5 多轮对话也封装进 qwen_client.py（chat_multi_turn）。
- 用户已在百炼环境 `course_core/resources/utils/qwen_client.py` 中加入 `init_api_key()`，并靠 `sys.path.insert(0, ".../course_core/resources/utils")` 在 2.1 notebook 中成功 import 调用。
